<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doraemon Game</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Thai:wght@100..900&family=Press+Start+2P&display=swap" rel="stylesheet">
</head>

<body>
    <div class="container">
        <div class="title">
            <h1 class="press-start-2p-regular">Doraemon Game <span>Card</span></h1>
            <h3 class="noto-sans-thai">จำนวนไพ่ที่เหลือ <span id="remaining-cards"><%= remainingCards %></span></h3>

            <form action="/reset" method="GET">
                <button class="noto-sans-thai" type="submit">เริ่มใหม่</button>
            </form>
        </div>
        <div class="card">
            <div class="card-box" id="card-box">
                <% if (card) { %>
                    <form action="/next-card" method="GET">
                        <button type="submit" style="background: none; border: none; padding: 0;">
                            <img src="<%= card.pic %>" alt="card show" id="card-img">
                        </button>
                    </form>
                    <h3 class="noto-sans-thai">มึงได้การ์ด <span><%= card.name %></span></h3>
                    <p class="noto-sans-thai"><%= card.detail %></p>
                <% } else { %>
                    <img src="/images/over.png" alt="game end">
                    <h3 class="noto-sans-thai"><span>จบเกมแล้ว</span></h3>
                    <p class="noto-sans-thai">กดเริ่มใหม่ถ้าไม่นิ่ม</p>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
